import type { ThemeDefinition } from "@/themes/types";
const t: ThemeDefinition = { id:"y2k", name:"Y2K", description:"2000s nostalgia with chrome, bubbles, and iridescent gradients", preview:"💿", defaultPaletteId:"candy-pop" };
export default t;
