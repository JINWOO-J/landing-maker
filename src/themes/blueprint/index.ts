import type { ThemeDefinition } from "@/themes/types";
const blueprintTheme: ThemeDefinition = { id: "blueprint", name: "Blueprint", description: "Technical blueprint with grid lines and wireframes", preview: "📐", defaultPaletteId: "ocean-deep" };
export default blueprintTheme;
