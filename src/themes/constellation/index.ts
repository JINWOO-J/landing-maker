import type { ThemeDefinition } from "@/themes/types";
const t: ThemeDefinition = { id:"constellation", name:"Constellation", description:"Star maps with connecting constellation lines", preview:"⭐", defaultPaletteId:"deep-space" };
export default t;
