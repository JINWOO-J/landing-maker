import type { ThemeDefinition } from "@/themes/types";
const t: ThemeDefinition = { id:"collage", name:"Collage", description:"Mixed media scrapbook with torn paper and tape", preview:"📎", defaultPaletteId:"peach-cream" };
export default t;
