import type { ThemeDefinition } from "@/themes/types";
const origamiTheme: ThemeDefinition = { id: "origami", name: "Origami", description: "Paper fold aesthetics with soft creases", preview: "🦢", defaultPaletteId: "warm-sand" };
export default origamiTheme;
