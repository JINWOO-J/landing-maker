import type { ThemeDefinition } from "@/themes/types";

const isometricTheme: ThemeDefinition = {
  id: "isometric",
  name: "Isometric",
  description: "Isometric grid with geometric shapes",
  preview: "🔷",
  defaultPaletteId: "electric-blue",
};

export default isometricTheme;
