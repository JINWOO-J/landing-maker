import type { ThemeDefinition } from "@/themes/types";
const t: ThemeDefinition = { id:"bento-grid", name:"Bento Grid", description:"Apple-style modular grid layout with clean boxes", preview:"🍱", defaultPaletteId:"slate-teal" };
export default t;
