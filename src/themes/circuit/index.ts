import type { ThemeDefinition } from "@/themes/types";
const circuitTheme: ThemeDefinition = { id: "circuit", name: "Circuit Board", description: "PCB traces, IC chips, and signal paths", preview: "🔌", defaultPaletteId: "neon-lime" };
export default circuitTheme;
