import type { ThemeDefinition } from "@/themes/types";
const t: ThemeDefinition = { id:"grain-film", name:"Grain Film", description:"Analog film grain texture with muted tones", preview:"🎞️", defaultPaletteId:"copper-rust" };
export default t;
