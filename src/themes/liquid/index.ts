import type { ThemeDefinition } from "@/themes/types";
const t: ThemeDefinition = { id:"liquid", name:"Liquid", description:"Organic flowing shapes with morphing blob animations", preview:"💧", defaultPaletteId:"violet-pink" };
export default t;
