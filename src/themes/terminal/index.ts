import type { ThemeDefinition } from "@/themes/types";

const terminalTheme: ThemeDefinition = {
  id: "terminal",
  name: "Terminal",
  description: "Hacker terminal with monospace aesthetics",
  preview: "💻",
  defaultPaletteId: "neon-lime",
};

export default terminalTheme;
