import type { ThemeDefinition } from "@/themes/types";

const particlesTheme: ThemeDefinition = {
  id: "particles",
  name: "Particles",
  description: "Floating particle system with connecting lines",
  preview: "✦",
  defaultPaletteId: "ocean-deep",
};

export default particlesTheme;
