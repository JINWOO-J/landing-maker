import type { ThemeDefinition } from "@/themes/types";

const geometricTheme: ThemeDefinition = {
  id: "geometric",
  name: "Geometric",
  description: "Abstract geometric shapes and patterns",
  preview: "🔺",
  defaultPaletteId: "candy-pop",
};

export default geometricTheme;
