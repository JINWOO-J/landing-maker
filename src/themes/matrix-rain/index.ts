import type { ThemeDefinition } from "@/themes/types";
const t: ThemeDefinition = { id:"matrix-rain", name:"Matrix Rain", description:"Falling code rain with green phosphor glow", preview:"🟢", defaultPaletteId:"toxic-green" };
export default t;
